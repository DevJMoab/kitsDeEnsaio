{% extends "layout.njk" %}

{% block head %}

<<<<<<< HEAD
    <title>Kits</title>
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">

{% endblock %}

{% block content %}

    <div class="titulo">
        <h1>Kits para Ensaio</h1>
    </div>

    <section class="cards">

        {% for item in items %}

            <div class="card" id="{{item.id}}">
                <div class="card_img_container {{'featured-wrapper' if item.featured }}  ">
                    <img src="{{item.img}}" alt="{{item.title}}">
                    {% if item.featured %}
                        <div class="featured">Novo</div>
                    {% endif %}
                </div>
                <div class="card_content">
                    <p>{{item.title}}</p>
                </div>
                <div class="card_info">
                    <p>{{item.duration}}</p>
                    {# <p class="card_price">{{item.price}}</p> #}
                </div>

            </div>

        {% endfor %}

    </section>
    <!-- <div class="modal-overlay"> <div class="modal"> <a class="close-modal"> <i class="material-icons">close</i> </a> <div class="modal-content"> <iframe src="" frameborder="0"></iframe> </div> </div> </div> -->
    <script src="/scripts.js"></script>
=======
<title>Kits</title>
<link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">

{% endblock %}


{% block content %}

<div class="titulo">
    <h1>Kits para Ensaio</h1>
</div>

<section class="cards">

    {% for item in items %}

    <div class="card" id="{{item.id}}">
        <div class="card_img_container {{'featured-wrapper' if item.featured }}  ">
            <img src="{{item.img}}" alt="{{item.title}}">
            {% if item.featured %}
            <div class="featured">Novo</div>
            {% endif %}
        </div>
        <div class="card_content">
            <p>{{item.title}}</p>
        </div>
        <div class="card_info">
            <p>{{item.duration}}</p>
            <p class="card_price">{{item.price}}</p>
        </div>

    </div>

    {% endfor %}

</section>
<!--
<div class="modal-overlay">
    <div class="modal">
        <a class="close-modal">
            <i class="material-icons">close</i>
        </a>

        <div class="modal-content">
            <iframe src="" frameborder="0"></iframe>
        </div>

    </div>
</div>
-->
<script src="/scripts.js"></script>
>>>>>>> 3d5f736675563e630b188875969d013b5382877f

{% endblock %}